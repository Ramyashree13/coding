string dayOfProgrammer(int year) 
{
    string res;
    ostringstream str1;
    int feb,i,sum=0,date;
    vector<int> days={31,28,31,30,31,30,31,31};
    if((year%4)==0)
    {
        replace(days.begin(),(days.begin()+1),28,29);
    }
    for(i=0;i<days.size();i++)
    {
        sum=sum+days[i];
    }
    date=256-sum;
    res=to_string(date);
    res.append(".09.");
    res.append(to_string(year));
    res=to_string(days[1]);
    return res;
}
